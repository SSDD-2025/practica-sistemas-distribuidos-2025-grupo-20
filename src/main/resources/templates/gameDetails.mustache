<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            display: block;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .container {
            text-align: center;
        }

        .settings-container {
            background: white;
            padding: 25px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            width: 300px;
            margin: auto;
        }

        button {
            background: #007BFF;
            color: white;
            border: none;
            cursor: pointer;
            width: 100px;
            height: 35px;
            border-radius: 5px;
            margin: 10px;
        }

        button:hover {
            background: #0056b3;
        }

        h3 {
            text-align: center;
        }
    </style>
</head>
<body>
<div class="container">
<div class="container">
    <h3>START TIME: {{gameModel.startTime}} </h3>
</div>
<p> players: </p>

<div class="container">
    {{#gameModel.userModel}}
        <div>
        {{username}}
        </div>
    {{/gameModel.userModel}}

</div>
<div>

</div>
<div>


    <form method="post" action="/user/games/details">
        <input type="hidden" name="userId" value={{userModel.id}}>
        <input type="hidden" name="gameId" value={{gameModel.id}}>
        <button type="submit"  value="join">{{status}}</button>

    </form>
    <form method="get" action="/user/pitch/games">
        <input type="hidden" name="userId" value={{userModel.id}}>
        <input type="hidden" name="pitchId" value={{pitchModel.id}}>
        <button type="submit" value="Return">Return</button>
    </form>
</div>
</div>
</body>
</html>